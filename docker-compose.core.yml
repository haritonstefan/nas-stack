services:
  homepage:
    image: ghcr.io/gethomepage/homepage:v1.13.2
    container_name: homepage
    restart: unless-stopped
    environment:
      HOMEPAGE_ALLOWED_HOSTS: ${HOMEPAGE_ALLOWED_HOSTS:-apollo.local}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-10}
      TZ: ${TZ:-Europe/Bucharest}
    # Homepage runs as PUID:PGID (1000:10), which cannot connect to the
    # root:docker 0660 socket. Reading labels needs the host docker group.
    group_add:
      - "${DOCKER_GID:-121}"
    ports:
      - "${HOMEPAGE_HTTP_PORT:-80}:3000"
    volumes:
      - ${HOMEPAGE_CONFIG_DIR:-/volume2/docker/homepage/config}:/app/config
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - nas-net
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    labels:
      - homepage.group=Core
      - homepage.name=Homepage
      - homepage.icon=homepage.png
      - homepage.href=http://apollo.local/
      - homepage.description=Service dashboard

networks:
  nas-net:
    name: nas-net
